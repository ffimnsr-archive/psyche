version: '3'
services:
  psyche-server:
    extends:
      file: common-services.yml
      service: psyche-server
    build:
      context: .
      dockerfile: ./docker/production/Dockerfile
    ports:
      - 80:80      
